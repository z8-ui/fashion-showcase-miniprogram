# 服装图片展示小程序 部署指南

> 微信原生小程序 + 云开发（云数据库 + 云存储 + 云函数）。
> 开源模板：按下面步骤操作一次即可上线，商家端上传的商品所有用户实时可见。

## 前置条件
- 小程序 AppID：在 mp.weixin.qq.com 注册后获取（个人主体即可，无需支付/电商资质）
  - 获取后在微信开发者工具中填写：项目根目录 `project.config.json` 的 `appid` 字段
- 微信认证审核通过（不影响开通云开发，但影响上线发布）

## 一、开通云开发
1. 微信开发者工具打开本项目根目录（导入项目，AppID 填你自己的）
2. 顶部工具栏点「云开发」→ 按提示开通
   - 新账号首次开通有免费额度（约 3 个月）；正式计费后基础套餐 19.9 元/月
3. 创建环境（如环境名 cloud1），记下环境 ID（形如 cloud1-xxxxxx）
   - 只有一个环境时，utils/config.js 的 CLOUD_ENV 留空即可（用默认环境）

## 二、创建数据库集合
云开发控制台 → 数据库 → 创建集合：
1. `products` 商品集合
   - 权限设置：所有用户可读，仅创建者可读写（写操作实际走云函数，读对所有用户开放）
2. `admins` 管理白名单集合
   - 权限设置：仅创建者可读写（默认即可，别开放给普通用户）

## 三、部署云函数
1. 开发者工具左侧资源管理器找到 cloudfunctions/admin 目录
2. 右键 → 「上传并部署：云端安装依赖」
3. 部署成功后控制台应显示 admin 云函数

## 四、添加管理白名单（关键！）
管理页只允许 admins 集合里的 openid 进入：
1. 让商家用手机微信打开小程序任意页面一次
2. 云开发控制台 → 用户管理，找到商家微信对应的 openid，复制
3. 云开发控制台 → 数据库 → admins 集合 → 添加记录：
   { "openid": "粘贴商家的openid" }
4. 开发者自己也要加：自己的微信同样打开一次小程序 → 用户管理里复制自己的 openid → 加入 admins
   （这样开发者后续也能进管理页维护商品）

## 五、商家上传流程
首页底部 → 商家管理入口 → 选专区/组别 → 填名称 → 拍照或相册选图/视频（最多3个）
→ 保存。上传的图片视频存云存储，所有用户手机实时可见。

## 六、上线发布
1. 微信认证审核通过后，在 mp.weixin.qq.com 把小程序名称改成你自己的品牌名
2. 开发者工具 → 上传 → mp.weixin.qq.com 提交审核 → 发布

## 常见问题
- 管理页提示「无权限」：openid 还没加入 admins 集合，按第四步操作
- 商品列表为空：确认 products 集合权限为「所有用户可读」，且商家已上传过商品
- 云函数报错：确认已右键部署 admin 云函数（云端安装依赖）
- 想切换回本地演示：utils/config.js 的 DATA_SOURCE 改为 'local'，重新编译即可
